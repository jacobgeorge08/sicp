#lang sicp

;; Our initial formular for resistors is 
;; 1 / ((1/r1) + (1/r2))

;; When we take the LCM of R1 and R2 we get R1/R1 and R2/R2 
;; Since they arent equal to 1 and only an approximation
;; This leads to the inconsistency in answers
